-- =============================================================================
-- Seed data
--
-- Starter cards are no longer global: they are copied to each new user by the
-- `on_auth_user_created` trigger (see migration 20260918120000_auth_multiuser).
-- Sign up inside the app to get a populated deck.
-- =============================================================================

-- Intentionally empty. Add local-only fixtures here when needed.
